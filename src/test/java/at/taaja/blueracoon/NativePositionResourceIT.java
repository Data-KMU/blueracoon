package at.taaja.blueracoon;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativePositionResourceIT extends PositionResourceTest {

    // Execute the same tests but in native mode.
}